import { describe, expect, it } from 'bun:test';

describe('AI', () => {
  it('should be able to intelligently make decisions to win a game', () => {
    const AIPlayer = new AIPlayer();
    const player = new Player('Player 1');

    const duel = new Duel();
  });
});
